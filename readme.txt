Readme
All the code is using python 2.7
You need to change the workdir in each program
you need to change the parameter in the first part if you want to change data
import numpy, pandas, 
AutoEncoderDemo is just to show the demo
Knn.py is the python program to knn algorithm
Autoencoder.py is the program of Autoencoder(the formal one)
Mf,py is the program of matrix factorization
sampledata.csv is the sample data the first column is the user id, the second column is the movie id, the third one is the rating value.
The loss is the MSE(MEAN SQUARE ERROR)


